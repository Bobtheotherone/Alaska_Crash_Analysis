# Unresolved author actions — r3 submission-readiness pass (2026-07-15)

Every `[AUTHOR ACTION REQUIRED: …]` marker printed in the manuscript, plus the standing
author-side items. Items 1–5 are the markers visible in the PDF; each names exactly
what is needed. None can be resolved by the repository or by any automated process.

## Markers printed in the manuscript

1. **Public repository URL** (§12.2 Code, data, and evidence availability). Provide the
   public URL hosting release tag `portfolio-final-r3`, or state that access is
   by-request only. The repository currently exists as a local checkout plus the
   handoff's provenance git bundle.
2. **Archival DOI** (§12.2). Register a DOI for the release (e.g., via an archival
   deposit) or state that none will be issued.
3. **Raw-hash publication permission** (§12.2). Confirm whether the licensed data
   agreement permits publishing the raw source workbook's SHA-256 in the availability
   section; the hash is recorded in the project's reproduction log.
4. **Reproduction contact** (§12.2). State the preferred contact or institutional
   procedure for authorized reproduction requests.
5. **External-AI privacy attestation** (§12.4 Authorship and AI assistance). Complete
   §§2–3 of `paper/AI_USE_AND_PRIVACY.md` (tools/services used; what project material,
   if any, was supplied to external generative-AI services) and replace the manuscript
   marker with the verified privacy statement. This cannot be reconstructed from the
   repository.

## Standing author-side items (also listed in `unresolved_external_evidence.md`)

6. **AUTHOR-001 human checklist** (pre-submission read-through and sign-off by the
   author).
7. **Departmental template approval** — confirm with the faculty mentor / graduate
   coordinator that the UAA-informed layout satisfies current requirements
   (`unresolved_external_evidence.md` row 9).
8. **Send the custodian request** (`paper/custodian_semantics_request.md`, drafted and
   ready) — blank-severity semantics, per-field recording times, prediction moment,
   crash-keyed pseudonymous agency re-supply.
9. **Cache the cited external sources locally** before archival submission
   (`unresolved_external_evidence.md` row 8; nhtsa.gov/highways.dot.gov block
   automated retrieval).

External-evidence items that are NOT author actions (custodian codebook, later cohort,
independent replication, stakeholder utility matrix) remain in
`unresolved_external_evidence.md` and are disclosed in the manuscript.
