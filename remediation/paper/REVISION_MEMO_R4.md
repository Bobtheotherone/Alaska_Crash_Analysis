# Revision memorandum — r4 final-submission pass (2026-07-16)

Baseline: r3 (`portfolio-final-r3` = commit `111f6f9`, PDF sha256
`ad417e67…`, 56 pp). Target: r4 (`portfolio-final-r4`). Every change is
additive editorial / documentation / packaging / repository-polish /
verification work; **no governed number changed and no frozen artifact was
touched** (frozen/locked gate 34/34; see `NUMERICAL_VERIFICATION_R4.md`).
"Impact" below means governed-result impact.

| # | Location | r3 text or state | r4 text or state | Reason | Evidence | Impact |
|---|---|---|---|---|---|---|
| 1 | `main.tex` §12.2 (availability, marker 1) | `[AUTHOR ACTION REQUIRED: provide the public repository URL …]` | Public repository URL and release URL stated: `github.com/Bobtheotherone/Alaska_Crash_Analysis`, tag `portfolio-final-r4`, exact commit/hashes in the release manifest | Repository is being published; marker resolvable with fact | verifier REQUIRED "availability repository URL" + "availability release URL"; PDF text scan | none |
| 2 | `main.tex` §12.2 (marker 2, DOI) | `[AUTHOR ACTION REQUIRED: register an archival DOI … or state that none will be issued.]` | Truthful statement: "No archival DOI had been issued at the time this submission package was finalized. The tagged public release is dated July 20, 2026." | No Zenodo credential exists on this workstation; a DOI may not be fabricated; 07/20/2026 is a release date, not a DOI | verifier REQUIRED "no-DOI statement" + "release date stated"; `AUTHOR_ACTIONS_R4.md` records the optional post-publication DOI action | none |
| 3 | `main.tex` §12.2 (marker 3, raw hash) | `[AUTHOR ACTION REQUIRED: confirm whether the licensed data agreement permits publishing the raw source workbook's SHA-256 …]` | Conservative resolution: hash retained in the restricted reproduction log, not published (permission unconfirmed under the NDA/data-use agreement); licensed holders still verify via the committed content hash | Permission not confirmed; conservative contractual choice | verifier REQUIRED "raw-hash withholding statement" + §9a scan (tex+pdf contain neither the value nor its prefix) | none |
| 4 | `main.tex` §12.2 (marker 4, contact) | `[AUTHOR ACTION REQUIRED: state the preferred contact …]` | "Questions … may be directed to Radames Naythan Mercado-Barbosa at rnmercado@alaska.edu. Access to the licensed source records remains controlled by the data owner; the author cannot grant source-data access independently." | Author-supplied contact; no implied DMV access | verifier REQUIRED "reproduction contact email" | none |
| 5 | `main.tex` §12.4 (marker 5, AI privacy) + `paper/AI_USE_AND_PRIVACY.md` §3 | `[AUTHOR ACTION REQUIRED: document … what project material was supplied to external generative-AI services …]`; attestation fields open | Author attestation recorded verbatim in substance: sensitive source records processed only in local computing environments by locally executed models/code; no NDA-protected source data transmitted to nonlocal LLMs or external AI services; external AI assistance limited to code/doc excerpts, synthetic examples, aggregates, manuscript text, de-identified artifacts | Author supplied the attestation for r4; deliberately scoped (distinguishes restricted records from code/aggregates; does not overclaim about internet-connected software) | verifier REQUIRED ×2 (local-only processing; nonlocal boundary); `PRIVACY_AND_NDA_ATTESTATION_R4.md` | none |
| 6 | `main.tex` §12.2 first paragraph | "corresponds to repository release tag `portfolio-final-r3` … manifest … **that travels with this PDF**" | "… tag `portfolio-final-r4` … manifest … **published as assets of that tagged release alongside this PDF**" | r4 supersedes r3 as the release of record; distribution model is now release-asset-based | verifier locked value `portfolio-final-r4`; FORBIDDEN check: r3 tag no longer named in the PDF | none |
| 7 | `main.tex` Table 5 tabnote | "the self-contained verification package **accompanying this manuscript**" | "… **available as an asset of the tagged public release, and also supplied alongside this PDF when the receiving system permits multiple files**" | Phase-6 package-availability accuracy | verifier REQUIRED "asset of the tagged public release" | none |
| 8 | `main.tex` §12.1 CI sentence | "A continuous-integration workflow is defined and has been run locally, but no hosted green status is claimed." | "A continuous-integration workflow for the no-license verification tier is defined in the repository; any hosted execution status is visible on the public repository rather than claimed here." | Future-proof truthfulness: statement stays correct before and after hosted CI runs | statement present in tex/PDF; workflow at `.github/workflows/verify-portfolio.yml` | none |
| 9 | Tables 11/12, 14/15, 17/18 (r3 numbering) | Continuation pages held **new table numbers** but were suppressed from the List of Tables → LoT visibly skipped 12, 15, 18 | `\ContinuedFloat` (caption package) on each part 2: same number as part 1, caption "…part 2 of 2 (continued)", no LoT entry; subsequent tables renumber automatically; final LoT contiguous 1–17 | Professional continuation mechanics (r4 directive Phase 5) | build log: 0 undefined refs; verifier §9b: 3 continuations, contiguous LoT, contiguous PDF table numbers; no `\ref` targeted the old part-2 numbers (checked) | none |
| 10 | `main.tex` line ~347; generated feature-governance tabnotes | "placeholder tokens", "missing/placeholder values" | "sentinel tokens", "missing/sentinel values" | The r4 zero-marker scan bans the token "placeholder"; "sentinel" is the more precise term already used in the project's ledgers | expanded-source token scan: 8/8 marker tokens absent; gen_feature_governance_tables --check PASS | none (wording in notes; every numeric cell identical) |
| 11 | `main.tex` preamble | color names `PlaceholderRule`/`PlaceholderText`; comments say "placeholder" | `MissingFigRule`/`MissingFigText`; comments say "stand-in box" | Same zero-marker scan; internal names only | build reproduces identical page content (token diff at packaging) | none |
| 12 | `remediation/research/` provenance docs (8 files), `research_audit/claim_scan.py`, superseded `paper/final_paper.md`/`.html` | Licensed raw workbook SHA-256 stated (full value in 3 docs; 8-char prefix in 8 more) | All 11 files redacted to the withholding language; value moved to the local-only restricted reproduction log (`private_reproduction_log/`, gitignored); claim_scan expectation switched from the hash prefix to the withholding phrase | Publication permission for the source identifier is unconfirmed (F-R4-01); these files become public with the repository | `PUBLIC_REPOSITORY_AUDIT_R4.md` §5; residual scan zero occurrences outside the private log; claim scan PASS | none |
| 13 | `unresolved_external_evidence.md` item 10 | Author-only availability/disclosure items listed as open with visible markers | Marked RESOLVED in r4 with the resolution of each item; remaining external item = optional archival-DOI registration | Ledger accuracy | file diff; items map 1:1 to memo rows 1–5 | none |
| 14 | Root repository presentation | Root README described only the Iteration III application (with a pointer note) | New 15-section research-forward README (quick links, research question, main result, why it matters, critical limitations, lineage, structure, reproduction tiers, no-license command, licensed-data requirements, privacy/NDA, app setup, license scope, citation, contact); application docs preserved at `docs/APPLICATION_PLATFORM.md`; new `CITATION.cff` (version portfolio-final-r4, date-released 2026-07-20, **no DOI field** until one exists), `DATA_AVAILABILITY.md`, `LICENSES/README.md` (component-scoped licensing; MIT stays scoped to `remediation/`), `SECURITY.md`, root `REPRODUCIBILITY.md` pointer | Faculty-reviewer landing page (Phase 3); license scope must match actual rights | files in repo; README states all eight required caveat statements verbatim-equivalent | none |
| 15 | `.gitignore` (root) | Basic Python/Node ignores | NDA-reasoned exclusion categories: licensed workbook names + fail-closed `*.xls*`, raw/restricted dirs, restricted reproduction records, secrets (`.env*`, keys, credentials), `*.bundle`, `release/`, model caches | Phase-2 hardening; protects the restricted log and backup bundle | `git check-ignore` verification; zero tracked files caught | none |
| 16 | `tools/package_release.py`, `tools/build_handoff.py` | r3 packaging: handoff embedded a `--all` git bundle ("repository is local; NOT pushed") | r4: release metadata block (URL/tag/date/no-DOI); r4 documents staged; redline r3→r4 shipped; **fail-closed forbidden-content scans** (prohibited file types, secret patterns, raw-hash tokens sourced from the local-only restricted log); **no git bundle in the public package** — provenance = the public repository at the release tag | The handoff becomes a public release asset; a `--all` bundle would republish withheld local history (F-R4-01) | `PUBLIC_REPOSITORY_AUDIT_R4.md` §5 containment; packaging output logs | none |
| 17 | `tools/verify_manuscript_numbers.py` | 893 checks; REQUIRED expected the r3 tag and visible markers | 926 checks; r4 REQUIRED (URL, release URL, contact, no-DOI, date, withholding, attestation ×2, release-asset wording), FORBIDDEN marker tokens (8), locked tag → r4, §9a raw-hash withholding scan, §9b continuation/LoT checks; git gate now skips gracefully when the governed baseline commit is absent (shallow/snapshot checkouts) | Verifier must enforce the r4 state; CI robustness | verifier output 926/926 | none |
| 18 | CI | `crashsev-ci.yml` branch filter did not cover release branches; no portfolio verification workflow | `crashsev-ci.yml` branches extended (`portfolio-final-*`, default branch); new `.github/workflows/verify-portfolio.yml` running ONLY the no-license tier (pytest, 8 generator gates, frozen/locked, tex-side manuscript verifier, claim scan); never requires restricted data | Phase-3/10 public CI | workflow files; hosted status visible on the repository after push | none |
| 19 | Redline | `redline_r2_to_r3` (r3's own redline) | New `redline_r3_to_r4.tex/.pdf` (latexdiff-so --flatten, 56 pp; both sides flattened with the identical bibliography); r2→r3 redline retained | Required r3→r4 tracked-change deliverable | compiled PDF; 74 DIF markers correspond 1:1 to memo rows 1–11 | none |
| 20 | Release documents | r3 set (REVISION_MEMO_R3 …) | r4 set added: `REVISION_MEMO_R4.md` (this file), `PUBLIC_REPOSITORY_AUDIT_R4.md`, `PRIVACY_AND_NDA_ATTESTATION_R4.md`, `NUMERICAL_VERIFICATION_R4.md`, `ADMISSIONS_REVIEW_CHECKLIST_R4.md`, `AUTHOR_ACTIONS_R4.md`, `GITHUB_PUBLICATION_R4.md`, `RELEASE_WORKLOG_R4.md`; r3 documents preserved unchanged as historical record | Required r4 reporting | files present; shipped in the release dir and handoff | none |

Numeric-token consequence of this revision: the manuscript's *governed* numeric
tokens are unchanged; expected token-level differences vs the r3 PDF are
confined to (a) the three continuation table numbers and downstream table
numbers (20 printed numbers → 17), (b) the availability subsection's new
URL/date text, and (c) the removed marker strings. The packaging-time
`pdf_token_diff.py` gate enumerates the exact token diff vs the r3 PDF and the
result is captured in `pdf_token_diff.json`; any numeric-token change outside
the enumerated expectation fails the release.
