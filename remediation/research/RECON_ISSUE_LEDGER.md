# Reconnaissance issue ledger — independently verified

Dispositions the appended repository-wide reconnaissance report (review of the `5be0fbf` handoff,
archive SHA `22e40a7a…`) against the working tree, **verifying each finding in source before acting**
(authority order: executable evidence > reproduced results > VCS history > the report > prose).

An issue is **closed** only when its acceptance criterion is evidenced. Status key:
✅ fixed · 🟡 partial / bounded residual · ⚪ out-of-scope or blocked (documented) · ❌ rejected.

The reconnaissance is accurate: its four headline S0 findings were all reproduced in source. The
prior `5be0fbf` package shipped an empirical artifact generated by an **older commit (`bd8d0428`)**
than the code that fixed calibration selection, and made governance/weighting claims contradicted by
the implementation. This round regenerates the authoritative artifact from current code and corrects
the contradicted claims.

## P0 — the four contradicted major claims

| ID | Verified? | Status | Action & evidence | Residual |
|---|---|---|---|---|
| **GOV-001** | **Confirmed** — `prepare()` loads/maps/splits all years; the develop report carried `split.class_prevalence.final_test = {0:0.677,1:0.284,2:0.039}` and the run printed `final_test(sealed)=11630`. | ✅ | `develop_split_view()` strips the held-out cohort's class prevalence from development artifacts; runtime "sealed" strings relabelled (develop print, split-manifest note, calibration docstring). Guard test `test_develop_split_view_strips_final_test_prevalence`. Claim relabelled: development *fitting and selection* use 2009–2011 only and no 2012 outcomes enter development artifacts — **not** a hard technical air-gap. | A single process reading one CSV cannot *prove* non-access; the honest claim is procedural + artifact isolation. Whole-extract label counts remain as data-provenance. |
| **CAL-001** | **Confirmed** — frozen `final_results.json` generator = `bd8d0428`, which predates the CAL-SELECT fix (`62993c9`); the shipped calibration selection used the buggy path. | ✅ (regenerated) | Current code selects the calibration candidate from the frozen **development** report before any final access (`cli.py:567–580`, guard `test_select_best_candidate_uses_dev_cv`). Artifact regenerated from current commit. | Result-change vs the old artifact is reported in the final execution report (regenerated, not asserted). |
| **FAC-001** | **Confirmed** — `leakage_factorial.py` averages "main effects" over `_random_split` (IID, 33% test) and `_temporal_split` cells with different test populations/train sizes. | 🟡 | Reframed as **separate matched contrasts**, not isolated causal main effects: the leakage contrast holds train/test IDs+model+seed fixed (valid); the random-vs-temporal contrast is relabelled **validation-optimism** (different estimands), not a clean split "effect". Stale "sealed" note fixed. | A fully matched preprocessing-leakage negative control is identified; the existing factorial is retained as a *diagnostic*, not a causal decomposition. |
| **WGT-001** | **Confirmed** — every registry model uses `class_weight="balanced"`; there was **no** unweighted comparator, so the paper's weighted-vs-unweighted narrative had nothing to contrast. | ✅ | Added matched controls `random_forest_unweighted` / `ordinal_random_forest_unweighted` (`class_weight=None`, `kind="ablation"`), evaluated under identical seed/features/folds; guard tests ensure they never become the primary candidate. Paired weighted-vs-unweighted result reported from the regenerated run. | The weighting *mechanism* claim now stands or falls on the regenerated matched numbers (reported honestly). |

## P1 — defensibility

| ID | Verified? | Status | Action & evidence | Residual |
|---|---|---|---|---|
| **ART-001** | **Confirmed** — artifact commit `bd8d0428` ≠ release tag. | ✅ (regenerated) | Full develop→freeze→evaluate-final re-run from committed current code; new artifact records the current commit; development CV reproduced bit-exactly (all 11 prior models, diff 0.000000) before adding the ablation models. | From-raw (Route B) parity still needs the raw workbook (absent) — see PROV-001. |
| **HOLD-001** | **Confirmed** — 2012 exposed; no external timestamp. | 🟡 | Retrospective/exposed framing is already central (Route R); intervals are descriptive; no confirmatory/prospective language. | A genuinely untouched later/external cohort would be needed for a stronger claim (unavailable). |
| **PROV-001** | **Confirmed** — raw `.xlsx` absent; source-agency authority/codebook unverified; raw byte-hash unavailable. | ⚪ blocked | Disclosed in `DATA_AUTHORITY_AND_ACCESS.md`; governed content hash `059559cd…` is the stable identity; the modeling table **is** present locally, enabling metric-level regeneration (Route A) but not from-raw (Route B). | External data owner/agency required. |
| **REPRO-001** | **Confirmed** — only self-reproduction. | 🟡 | Metric-from-prediction and now current-code regeneration are demonstrated; labelled self-reproduction. | Independent third-party clean-room run outstanding. |
| **SPLIT-001** | **Confirmed** — `dev_mask = ~test_mask & notna`: development is *any* non-final year; no earlier-only/contiguity enforcement. | ✅ | Added an ordered-contiguity guard: development years must all precede the final years and be gap-free, else `ContractViolation`; negative test added. | — |
| **AUTH-001** | **Confirmed** — `schema.py` hard-codes `FeatureSpec("Functional Class", …)` twice (lines 169 & 189); two feature authorities (schema.py vs the 101-row ledger CSV) coexist. | 🟡 | De-duplicated `Functional Class`; documented that the **ledger CSV is the runtime authority** for allowed/prohibited features and `schema.py` is the Gate-0 availability tier source; a consistency test asserts the allowed set matches the ledger. | Full single-source unification (generate schema from ledger) deferred as larger refactor. |
| **CAL-002** | **Confirmed** — random `GroupKFold` calibration; single top-label ECE. | 🟡 | Documented as a diagnostic; proper scores (log-loss, Brier, RPS) are already reported alongside ECE. | Temporal out-of-fold calibration + classwise reliability deferred (bounded). |
| **METRIC-001** | **Confirmed** — undefined precision/recall/F1 → `0.0` at `metrics.py:48–50`. | ✅ | Now NaN + per-class `status`; macro-F1 NaN when any class F1 undefined; balanced accuracy unaffected (recall defined). Guard `test_undefined_precision_is_nan_not_zero`. | Report/table renderers show "undefined (never predicted)". |
| **DEC-001** | **Confirmed** — `decision_rule_sensitivity.md` named a generator that did not exist; "sealed" label stale. | ✅ | Wrote `crashsev/decision_rule_sensitivity.py`; it reproduces the shipped table exactly (ordinal RF 0.3310/0.3303; RF 0.3436/0.3607; majority 0.3614) and relabels the cohort held-out out-of-time. | — |
| **MODEL-FAMILY / MODEL-001** | Confirmed — EBM was final-only; PO optimizer success stored but not enforced. | 🟡 | EBM is absent from the frozen environment (optional dep not installed) so it does not enter the regenerated comparison; documented as exploratory. | Enforcing PO non-convergence-as-failure and freezing optional-model availability by config deferred. |
| **CV-001 / STAT-001 / OBJ-001 / LIT-001** | Confirmed | ⚪/🟡 | Bounded and disclosed: two rolling folds (data has 3 dev years); crash-level bootstrap under a stated independence approximation; oMAE declared a proxy with no invented practical margin; literature gap acknowledged. | Stakeholder cost, block/refit uncertainty, and expanded primary-literature review require external input/time. |
| **CI-001 / DEP-001** | Confirmed | ⚪ blocked / 🟡 | CI configured + run locally; not pushed → no hosted run claimed. Dependency lock is direct-only (documented, not called a full transitive lock). | Push authorization / hashed transitive lock outstanding. |

## P2 / P3

| ID | Verified? | Status | Action & evidence | Residual |
|---|---|---|---|---|
| **SYN-001** | Confirmed — `runs/` co-locates a synthetic run (`is_synthetic=true`) with the real final run. | 🟡 | `is_synthetic` flags already present in every manifest; documented which run is empirical. | Physical `runs/synthetic/` vs `runs/empirical/` segregation deferred (low risk given flags). |
| **PREP-001** | Confirmed — high-cardinality screening once on all dev data before folds (no feature was actually dropped in the released run). | 🟡 | Documented as structural; no released feature was affected. | Moving screening inside fold pipelines deferred. |
| **METRIC/PERF/DOC/LEGACY/REAN/AI/ERR/CONTRACT/TARGET/DATA** | Confirmed as described | 🟡 / ⚪ | Disposed individually in `ISSUE_DISPOSITION_CURRENT.md`; the highest-value P2s addressed this round are METRIC-001, DEC-001, SYN-001 labelling. | Error-analysis expansion, resource RSS, full contract hardening, target-exclusion sensitivity, and applicant defense pack are bounded/external. |

## Summary

All four P0 contradictions are corrected in code (GOV-001, WGT-001) or by regenerating the artifact
from current code (CAL-001, ART-001); FAC-001 is reframed honestly. Development CV reproduces
bit-exactly. The remaining items are bounded with disclosed residuals or blocked on external inputs
(raw data, push authorization, stakeholder cost, independent reproduction). No issue is closed on
wording alone; the regenerated headline numbers are reported in the final execution report.
