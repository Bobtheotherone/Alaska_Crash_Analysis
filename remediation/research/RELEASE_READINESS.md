# Release Readiness

Gate status for the portfolio finalization (master-prompt §16). ✅ pass · 🟡 pass-with-residual ·
⛔ blocked.

| Gate | Status | Basis |
|---|---|---|
| **A — truth & scope** | ✅ | Route R explicit (`ROUTE_DECISION.md`); no sealed/preregistered/independent overclaim in paper/README/handoff; data authority neutral and bounded; authorship + AI assistance disclosed. |
| **B — method & code** | 🟡 | Calibration selection development-only + guard test; decision rule defined & tested (argmax primary in v3.x — posterior-median primary is the v4 change); fixed registry with roles; class-weight trade reported via matched controls; target/split contracts enforced; historical failure modes have regression tests; learned preprocessing is fold-local (feature *typing* is not — PREP-001, v4). **Correction (third round):** the "deterministic prevalence probability baseline" previously claimed here does not exist — `empirical_prior` is stochastic draws (BASE-001); a true `strategy="prior"` baseline is a v4 addition. |
| **C — statistics** | ✅ | One primary comparison; secondary/exploratory labeled; no multiplicity claim beyond primary; interval + effect size in intuitive units; no invented practical threshold; rare severe-class denominators reported. |
| **D — paper** | ✅ (re-fixed v3.1) | Every headline number traces to an artifact (`CLAIM_EVIDENCE_MATRIX.md`); limitations include exposure + retrospective amendment + self-reproduction + GOV-001 artifact derivability + FEAT-001 timing; author set. **Third-round incident:** the v3 PDF shipped renderer-injected "sealed/grouped-bootstrap" figure captions and three §7 claims contradicted by the artifacts (dev ranking, proper-score superlative, "generated by this tagged commit itself") — all corrected in v3.1, and the claim scan now covers rendered HTML output. |
| **E — reproducibility** | 🟡 | Route A reproduces from the delivered package; Route B fully specified; all files hashed; run artifacts immutable/non-overwriting; **self-reproduction** labeled accurately; raw-file byte hash **recorded** and the raw→table→frozen-hash chain verified (2026-07-11); the run bundle's cryptographic skeleton + de-id witness committed (`evidence_release/`). Residual: no independent reproduction; lock is direct-pins only (LOCK-001). |
| **F — handoff** | ✅ | Standalone package with `00_START_HERE`, issue disposition, claim matrix, manifest, verifier `09_VERIFY_HANDOFF.py`; a fresh extraction self-verifies; unresolved issues visible; nothing falsely closed. |

## Residual blockers (all disclosed)
* **PROV-EXT-001 / CI-PUBLIC-001** — no push authorization → no external timestamp, no hosted CI.
* **DATA-AUTH-001** — source-agency provenance unverified (raw-file byte hash now recorded in the restricted reproduction log; withheld from public release; custodian/codebook evidence remains external — `PROVENANCE_ACQUISITION_PLAN.md`).
* **CLASS-WEIGHT-001** — matched same-model weighting ablation **now run** (WGT-001; two `*_unweighted`
  controls). **MISSING-001** — high-missingness-feature ablation still not rerun (documented).
* **Reconnaissance input** — the second-round findings were supplied **inline** in the prompt and
  verified against source; the separately named `Current_HEAD_Reconnaissance` file (with an expected
  SHA) was absent as a standalone artifact, so its hash could not be checked.
* **Human action** — applicant must complete the responsibility checklist in
  `AUTHORSHIP_AND_AI_ASSISTANCE.md`; independent faculty review is external.

**Verdict (v4.1):** the corrected-protocol benchmark plus the fourth-round correction cycle are
**executed, frozen, and packaged**: the two central result-interpretation errors are fixed and
machine-checked (the beats-prior set is computed from the artifact; severe hard-decision recall
and severe ranking signal are reported as distinct findings), the governance language states its
exact boundary (semantic/analytic isolation, not byte-level non-access), the tier and target are
named as author-judged/researcher-defined constructs, all four prespecified sensitivities are
frozen and reported (broad tier; high-missingness — under which the primary contrast survives in
direction but **attenuates** to −0.0091 [−0.0181, −0.0001], a stated dependence; target-mapping
estimand collapse; five-seed refit robustness), H2's failure is reported as a result, figures are
legible, the PDF is paginated, and `research_audit/RELEASE_STATE.md` is the machine-generated
authoritative record. **Open items:** the applicant's responsibility checklist
(`AUTHORSHIP_AND_AI_ASSISTANCE.md` — human-only), and the Gate-3 external evidence
(`PROVENANCE_ACQUISITION_PLAN.md`: custodian attestation, official codebook — construct-critical,
field-timing evidence, push→hosted CI + external timestamp, independent reproduction). Gate 3 is
**initiated** (raw byte identity + chain verified), not complete. Not prospective, not publicly
timestamped, not independently reproduced — and it does not claim to be.
